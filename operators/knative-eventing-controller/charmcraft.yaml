type: "charm"
bases:
  - build-on:
    - name: "ubuntu"
      channel: "20.04"
    run-on:
    - name: "ubuntu"
      channel: "20.04"
parts:
  charm:
    charm-python-packages: [setuptools, pip]
    # TODO: Temporarily using lightkube fork that supports batch-apply from git.  Remove this when reverting that
    #  dependency change.
    build-packages: [git]
